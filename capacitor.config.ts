import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kssc.espmobiletest',
  appName: 'esp-mobile-test',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
